# Runtime dependencies for the Intelligent Test Planning Agent.
# Pinned so container builds and Jenkins runs are reproducible.
# The HTTP layer uses the Python standard library - no web framework needed.

python-docx==1.1.2
openpyxl==3.1.5
